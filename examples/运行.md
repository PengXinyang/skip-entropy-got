python examples/batch_static_skip_experiment.py --model-name deepseek-v4-flash --tasks set_intersection_128,sorting_128 --data-ids 0,1,2 --skip-ratio 0.2 --skip-orders low high --entropy-field normalized_avg_entropy_bits

说明：指定 `--data-ids` 时会运行这些 id 的全部样例，不再受默认 `--max-cases 1` 限制。
